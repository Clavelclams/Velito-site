# Velito Cours — copier en .env.local (ignoré par Git).
# Mêmes valeurs Supabase que le hub/compta (projet velito partagé).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Liste blanche (OBLIGATOIRE en prod : auth.users est partagé avec tout
# Velito — seuls ces emails accèdent à l'espace de révision).
COURS_EMAILS_AUTORISES=
